name: web-local
version: 1.0.0
description: "Local URL extraction — httpx + BeautifulSoup for HTML, the local OCR stack for PDFs, optional Playwright rendering for JS-heavy pages. No API key, no third-party service."
author: skocks
kind: backend
provides_web_providers:
  - local
